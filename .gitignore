# Local environment variables and secrets (Do NOT push to Git)
local_env.json
.env

# Local development and temporary logs
*.local.md

# macOS
.DS_Store

# Scratch area (device_bash では削除できないため手動で削除すること)
_to_delete/
