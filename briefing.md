# briefing.md — プロダクト開発の原点ドキュメント

> 本ドキュメントは `local_env.json`（Git追跡外）にバインドされたパラメータと、[FigJamボード](https://www.figma.com/board/fNzEJfMeOIke16NmCuqKLa/aidlc-figjam) をMCP経由で解析した内容を「単一の真実のソース（Single Source of Truth）」として生成されました。
> 以降生成されるすべての仕様ドキュメント（requirements.md / design.md / Guidelines.md / tasks.md / figma_make_prompt.md / README.md）は、本ドキュメントに矛盾しない範囲でのみ意思決定を行います（`prompts/inception_briefing.md` 6章ガードレール準拠）。

## プロジェクト設定パラメータ（local_env.jsonからバインド）

| 項目 | 内容 | 出典 |
|---|---|---|
| PROJECT_NAME | **ParentSync** | local_env.json |
| PROJECT_SUMMARY | 育児中の夫婦向けのLINE風1文自動仕分けアシスタント | local_env.json |
| FIGJAM_URL | https://www.figma.com/board/fNzEJfMeOIke16NmCuqKLa/aidlc-figjam | local_env.json（取得成功） |
| RAW_HEARING_MEMO | MCP経由で上記URLを参照 | local_env.json |
| 対象ユーザー | 育児中の夫婦 | ユーザー確認済み（2026-08-08セッションで確定） |

---

## 1. プロジェクトの背景と原点（Why & Who）

### 対象となる関係性・ユーザー像
育児中の夫婦。乳幼児の世話（授乳・オムツ・体温・寝かしつけ等）を交代・分担しているが、片方が対応している間、もう片方はその事実をリアルタイムに把握できない。この「今どちらが何をしたか」の非対称な情報格差が、特に深夜〜早朝の時間帯に集中して発生する。

### 課題の核心
既存の手段には以下の欠落がある。

- **LINEグループ／個人チャット**：会話が時系列に流れてしまい、育児記録として後から参照しにくい。定型フォーマットがなく、送る側の負荷が高い。
- **紙メモ**：手元にいない側（就寝中・外出中）には物理的に伝わらない。
- **口頭確認**：眠っているパートナーを起こしてしまう、または疲労している状態での会話自体が負荷になる。

いずれも「高頻度・深夜早朝集中・低認知負荷でなければ継続不可能」という利用文脈の要件を満たしていない。

### このプロダクトが存在すべき理由
深夜の育児対応を一文入力するだけで自動的に記録・仕分けし、パートナーと共有できる手段がなければ、育児負担の実態が可視化されず、どちらか一方（多くの場合、夜間対応を引き受けている側）に疲労と負荷が偏り続ける。これは「あったら便利」ではなく、放置すれば当事者の心身に継続的な悪影響を与える課題である。

---

## 2. リアルな利用文脈（Context of Use）

### 代表的な利用シーン
深夜2時、薄暗い寝室。片手は授乳中でふさがっている。画面を長時間見られない。操作は片手・数秒・1アクションで完結しなければならない。通知音を出せない環境。バイブレーションも最小限。

### UIの必然性 — 入力モデル
LINEライクな「雑な1文テキスト入力」である理由：フォームの多段入力はこのシーンで認知的に不可能。自由入力→自動仕分けのモデルのみが成立する。

### UIの必然性 — ビジュアルデザイン
パステルカラー・高輝度テーマの排除理由：暗順応した眼球への刺激を避けるため、ダークテーマ（背景輝度15%以下）が生理的要件として必須。デザインの好みではなく安全性の問題。

---

## 3. 引き算の設計判断（Design Decisions）

### 採用：HTML/JS 単一ファイル構成
- デプロイ環境ゼロ — ファイルをダブルクリックするだけで動く。
- ステークホルダーへの共有がメール添付1通で完結する。
- プロトタイプ段階でインフラ構築コストを負担する合理的理由がない。

### 見送り：Next.js / React / バックエンドサーバー
- ビルドパイプライン・Node環境・デプロイ設定の学習コストが検証の本質ではない。
- 仮説が検証されてから導入を検討する。今ではない。

### 判断基準の原則
「このコードは検証のために書くのか、製品のために書くのか」を常に問う。プロトタイプで製品品質を求めることは、検証速度の敵である。

---

## 4. セキュリティと安全性の要求

### 絶対禁止事項 — APIキーのソースコード永続化
APIキーをHTMLファイル・JSファイルにハードコードすることを禁止する。理由：ファイル共有・GitHubアップロード・スクリーンショット時に即座に漏洩する。許可される保持場所：ブラウザの `sessionStorage` のみ。タブを閉じると自動消去される。

### 必須実装 — ローカル正規表現フォールバックモック
APIキー未入力時・API呼び出しエラー時は、外部通信を一切行わない。入力テキストをブラウザ内の正規表現ルールで自動仕分けし、UIとしての動作を継続する。目的：デモ環境・オフライン環境でも動作することで、検証機会を損失しない。

### この設計が示すセキュリティ哲学
「動けばいい」ではなく「動き続けても安全である」を設計の前提とする。フォールバックの存在はUX品質の担保でもあり、セキュリティリスクの封じ込めでもある。

---

## Assumptions（推測により補完した事項）

- FigJam原本セクション1（背景・原点）は取得時点で「インプットデータ未入力の構造テンプレート」であり、対象ユーザー像は「育児中の夫婦」「介護者と被介護者」等の複数例を並記した状態だった。`local_env.json` の PROJECT_SUMMARY 記述、および先行セッションでのユーザー回答により「育児中の夫婦」に確定。介護シナリオは今回のスコープ外と解釈している。
- 「課題の核心」「このプロダクトが存在すべき理由」の具体的な文章は、確定した対象ユーザー像とセクション2〜4（利用文脈・設計判断・セキュリティ要件）の記述内容から論理的に導出したものであり、FigJam原本に一字一句の記載があるわけではない。

## Open Questions（不足情報・要確認事項）

- 自動仕分けのカテゴリ体系（例：授乳／睡眠／体温／服薬／その他）の具体的な分類ラベルは、FigJam原本・local_env.json のいずれにも明記されていない。requirements.md 生成時にMust要件として定義が必要。
- 外部API（自動仕分けに使う想定のAI API）の提供元・モデル名は未指定。README.md / design.md 生成時に「利用者が任意のAPIキーを設定する」前提で良いか確認が必要。
- 記録の永続化要否（sessionStorage以外の保存、例：localStorageでの育児ログ蓄積）が明記されていない。セキュリティ要件はAPIキーの保持場所のみを規定しており、育児記録データそのものの永続化方針は未確定。
