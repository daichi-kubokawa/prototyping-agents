# review_history.md — レビュー履歴（追記専用）

## 第1回商談（DRAFT・2026-08-11）

- レビュー日時: 2026-08-11 23:18
- 対象コミット: 02174751e18ad3188bc9a418bae0eb299f24b905
- モード: DRAFT（advisory）

## 保留指摘（DRAFT）

DR-2b advisory スキャン（オーケストレータ実施）の結果。

| # | 検査 | 判定 | 根拠 | 対策案 |
|---|---|---|---|---|
| S-1 | 解法の混入 | PASS | `briefing.md` / `requirements.md` に配色・レイアウト・技術構成の指定なし。D-01/D-02 は design.md でのみ導出 | ― |
| S-2 | 機密の平文 | PASS | 実URL・アクセスキー・個人特定情報の平文混入なし | ― |
| S-3 | DOM バインド | PASS | `figma_make_prompt.md` の Constraints 節に禁止識別子リスト・DOM バインド規約を転記済み | ― |
| S-4 | C-3/C-4 の欠落 | PASS（推測で充足） | `requirements.md` C-3/C-4 は briefing.md に明示的記述がなく推測で埋め、OQ-1/OQ-2 として記録済み。完全な欠落ではない | ITERATE で確認 |
| S-5 | アップロード安全性 | **その場で修正済み** | `check_upload_safety.py` 初回実行で `requirements.md` に `briefing.md` への文字列参照が6件検出（FAIL）。全て識別子ベースの記述へ書き換え、再実行で PASS（検出0件）を確認 | 完了 |

**保留指摘（次回持ち越し）: なし。** S-5 のみ検出があったが、その場で修正し解消済み。

### ITERATE 消化（2026-08-12）

| # | DRAFT 時判定 | ITERATE での判定 | 根拠 |
|---|---|---|---|
| S-1〜S-3, S-5 | PASS | **消化・完了** | 差分なし。figma_output の実物照合（下記 IT-1-0）でも再検出なし |
| S-4（C-3/C-4 の欠落・推測で充足） | PASS（推測で充足） | **保留を維持** | 本パスでは新規のヒアリング情報が無く、OQ-1（装置）・OQ-2（物理環境）を確定させる材料がない。次回商談で確認するまで D-01/D-02 は既定値のままとする（§ Open Questions 参照） |

---

## 第1回商談（ITERATE・2026-08-12）

- レビュー日時: 2026-08-12 02:45
- 対象コミット: `1ee9536b21c7b9e0ae3a2339384e45125e30b553`
- モード: ITERATE（blocking）

⚠️ **顧客反復ログが空です。** DRAFT の出力は顧客の声を一度も経ていません。
本パスの検証は「内部整合性」に限定され、妥当性の検証にはなりません（下記「顧客反復ログの補完」参照）。

### IT-1-0: 最終出力との突き合わせ（`figma_output/01_2026-08-11` 実物）

対象: `IT Inquiry Management Prototype`（React + Vite、コード形式で回収済み）。

**静的検査（スクリプト、目視ではなく終了コード判定）**

| スクリプト | 対象 | 結果 |
|---|---|---|
| `check_forbidden.py` | `figma_output/01_2026-08-11/IT Inquiry Management Prototype` | PASS（禁止識別子 0件・5ファイル走査） |
| `check_upload_safety.py` | `.../src/imports/`（アップロード済み仕様のコピー） | PASS（検出0件・5ファイル走査） |
| `check_contrast.py --fix` 相当の再計算 | `Guidelines.md` | PASS（5件とも要求比率を満たす） |

**トークン値の突合**：`index.css` の `:root` 変数と `Guidelines.md` のカラートークン・タイポグラフィ・スペーシング・ターゲット寸法は**全項目で完全一致**。仕様との乖離は検出されなかった。

**`content.md`（T-01〜T-18）との突合**：18件全てが実際の画面文言として使用されていることを確認（欠落・誤記載なし）。

**記録漏れ（※事後検出）―― `content.md` に無いが画面に出ている文言**

`figma_make_prompt.md` の制約「`content.md` に無い文言が必要になった場合は生成せずに明示する」に対し、Figma Make は明示せずに以下を生成した。うち一部は構造的なフィールドラベル（要 content.md 追記）、一部は `content.md` の想定範囲外の**新規状態値**であり、後者は上流（`requirements.md` / `screens.md`）にも記述が無い。

| 検出文言 | 分類 | 対応 |
|---|---|---|
| 件名・依頼者・チャネル・受信日時・問い合わせ概要・対応履歴・メモ・担当者・対応状況・引き継ぎ先・引き継ぎメモ・類似の過去問い合わせ（見出し） | フィールドラベル（構造語彙） | `content.md` へ追記（IT-1-5 で design-agent が実施） |
| キャンセル・登録する・すべて・対応済みを含む・設定なし | 操作・選択肢ラベル | 同上 |
| **「未割り当て」（担当者の初期状態）** | **新規状態値・上流未記載** | ※後述。`requirements.md`/`screens.md`/`content.md` のいずれにも存在しない概念。保留指摘へ計上し人間へ確認（下記） |

**仕様との乖離**：なし（トークン値・振る舞い条件とも一致）。

**写像表に無い変更**：なし（顧客反復自体が発生していないため対象外）。

**新たに検出した保留指摘（IT-1-0 起因）**

| # | 検出箇所 | 種別 | 内容 | 判定 |
|---|---|---|---|---|
| 6 | `figma_output` App.tsx（担当者フィールド） | 仕様漏れ（V-3 相当・スコープクリープ） | 担当者が「未割り当て」になり得るケース（新規登録時、口頭受付分など）が `requirements.md` Must Have・`screens.md` の表示要素/条件分岐のいずれにも定義されていない。Figma Make が実装上の必要から独自に補った状態。「引き継ぐ」ボタンが未割り当て状態にも同一ラベルで表示されるのも、M-23（対応途中の引き継ぎ）を初回割り当てに流用した結果であり、意味的にやや不正確 | **要・人間確認**。次回商談で「未割り当て状態の扱い」「初回割り当てと引き継ぎを同一操作にしてよいか」を確認する。確認までは `requirements.md` Open Questions へ OQ-6 として追記し、`content.md` には値として記録するに留め、確定仕様としては扱わない |
| 7 | S01 検索・フィルタ | 軽微な仕様縮小 | `screens.md` は「対応状況での絞り込み」を表示要素に含めるが、実装は「対応済みを含む」トグルのみ（対応状況の個別フィルタは無し）。段階的開示の記述（対応済みは既定非表示）とは整合しており矛盾ではないが、フィルタ要求全体に対しては部分実装 | 情報として記録。Must Have の充足は「対応済み」の分離のみで足りるため FAIL とはしない。ITERATE の screens.md 補完時に表現を精緻化する |

### IT-1-1: 顧客反復ログの補完

**補完対象データなし。** 本商談では `/log` による反復記録が1件も作成されなかった
（DR-3 のフェーズでお客様への提示・改修依頼のサイクルが発生しなかった、または記録されなかった）。
時刻・所要時間・上流への反映内容は、記録が存在しない以上、事後に創作することはできない。
`## 顧客反復ログ` の表は空のまま確定する。

### IT-1-2: 仕様変更の上流反映

反映対象なし（顧客反復ログが空のため、確定した仕様変更が存在しない）。

### IT-1-3: 保留指摘（DRAFT）の消化

上記「ITERATE 消化（2026-08-12）」表のとおり。S-1〜S-3・S-5 は完了、S-4 は保留維持、
IT-1-0 起因の新規指摘（#6, #7）を追加計上した。

## 顧客反復ログ

DR-3（Figma Make ループ）実施時に `/log` で追記する。

| # | お客様の言葉 | 写像先 | 変更内容 | 所要 | 上流への反映 |
|---|---|---|---|---|---|

**記録なし。** IT-1-1 参照。次回商談以降、本表に速記すること。

## 検証ゲート1: 上流ブリーフ ➔ 要件定義への移行前レビュー

- レビュー日時: 2026-08-12 02:51
- 対象コミット: `1ee9536b21c7b9e0ae3a2339384e45125e30b553`
- モード: ITERATE（blocking） / 実施者: quality-agent
- 対象: `briefing.md` → `requirements.md`

| # | 検査項目 | 判定 | 根拠（該当箇所の引用） | 対策案 |
|---|---|---|---|---|
| G1-1 | 利用文脈の抽出漏れ（C-1〜C-5） | **PASS** | C-1 は M-1, M-20, M-21 に、C-2 は M-4, M-5, M-20, M-23 に、C-5 は M-15, M-16, M-18 に由来し欠落なし。C-3（装置）・C-4（物理環境）は `briefing.md` 自体に明示記述が無く（OQ-1, OQ-2）、`requirements.md` §1/§6 は「明示的な記述なし」と明記したうえで Assumptions に格上げし OQ-1/OQ-2 として列挙している。**捏造でも黙殺でもなく、SSoT の「推測は Assumption、不足は Open Question」の手順どおりに処理されている**ため、抽出漏れではなく「情報が無いことの正しい記録」と判定する | 対策不要。ただし D-01/D-02 は非支配的のまま据え置かれており、次回商談で OQ-1/OQ-2 の実測確認を優先すること（DRAFT の S-4 判定を継続） |
| G1-2 | `briefing.md` 内部矛盾 | **PASS** | M-9「全ての問い合わせを一箇所で確認したい」と M-15「Slackを活かした運用にしたい」は一見緊張関係にあるが、`briefing.md` は両者を並記するだけで一方を確定的に優先しておらず、OQ-5 で「Slack連携かSlack移行回避かの意図確認が必要」と明示的に矛盾の可能性を切り出している。**矛盾を隠さず Open Question 化しており、内部矛盾として扱うべき記述の断定は無い** | 対策不要。OQ-5 の解消を次回商談の優先確認事項とする |
| G1-3 | 解法の混入（配色・レイアウト・技術構成の確定記述） | **PASS** | `briefing.md`（M-1〜M-24）・`requirements.md`（C-1〜C-5, MoSCoW, §6）を全文走査したが、色値・フォント名・レイアウト形状・技術スタックの確定記述は無い。D-01（Light）/D-02（デスクトップ標準配置）は `design.md` でのみ導出されており、上流2文書には解法が紛れていない | ― |
| G1-4 | 機密の混入 | **PASS** | `briefing.md` の出典は `[FigJam Board (Confidential)]` としてマスク済み。M-1〜M-24 に実URL・アクセスキー・個人を特定できる氏名等は無い（「従業員約300名」等の集合的記述のみ）。`requirements.md` にも同様の混入なし | ― |

**検証ゲート1 総合判定: PASS**（4項目すべて PASS。次フェーズ（要件定義の確定）への移行を妨げる欠陥なし）

---

## 検証ゲート2: 下流仕様・設計の相互整合性レビュー

- レビュー日時: 2026-08-12 02:51
- 対象コミット: `1ee9536b21c7b9e0ae3a2339384e45125e30b553`
- モード: ITERATE（blocking） / 実施者: quality-agent
- 対象: `requirements.md` ⇔ `design.md` ⇔ `screens.md` ⇔ `content.md` ⇔ `Guidelines.md` ⇔ `figma_make_prompt.md`

> `design.md` の D-03/D-04 および §4〜9 の該当節（中核データ構造・非機能要件・状態遷移図等）は
> テンプレート上「ITERATE パスで記述」のまま未記入であり、**未生成であって欠陥ではないため N/A** とする
> （`artifact-templates.md` §2 の DRAFT 列に照らして正しい状態）。フォールバック経路の一致検査も、
> 本案件はバックエンド連携なし（Task 記載）で外部依存が存在しないため **N/A**。

### V-1〜V-4 検査結果

| # | 検査種別 | 判定 | 内容 |
|---|---|---|---|
| V-1 | 矛盾（Contradiction） | **PASS** | D-01（Light）は Guidelines.md のカラートークン（`--bg #F7F7F5` 等）と一致。D-02（デスクトップ標準配置）は `figma_make_prompt.md` の優先順位4に転記され、screens.md のレイアウト記述（軸1〜2本、色/影で強調しない等）と矛盾しない。上流が禁止した事項を下流が採用している箇所、上流の要求を別の振る舞いに置き換えた箇所は検出されなかった |
| V-2 | トレーサビリティ断絶 | **一部 FAIL**（下記 G2-5, G2-7 参照） | D-01/D-02 は C-3/C-4 まで遡れる（PASS、詳細は重点検査項目）。一方、screens.md の表示要素の一部が content.md に対応する文言 ID を持たず、由来を辿れない箇所がある |
| V-3 | 孤児成果物（Orphan） | **一部 FAIL**（下記 G2-6 参照） | 要件 Must Have の一部（M-14, M-15）が screens.md の Elements/Actions に機能として反映されていない疑いがある。設計側にスコープクリープ（要件に無い項目の混入）は検出されなかった |
| V-4 | 用語・値の揺れ | **FAIL**（下記 G2-8 参照） | 「優先度」と「緊急度」が同一概念に対し文書間で混在使用されている。カラートークン・タイポグラフィ・スペーシング・ターゲット寸法の数値自体は文書間で完全一致（後述） |

### 個別検査項目

| # | 検査項目 | 判定 | 根拠（該当箇所の引用） | 対策案 |
|---|---|---|---|---|
| G2-1 | セキュリティ要件の貫通（M-18 個人情報保護） | **PASS** | `figma_make_prompt.md`「### 秘匿情報」に「社員名・端末情報等の個人情報はダミー値であっても実在人物を想起させない表記にすること」と明記。`design.md` §6・`Guidelines.md` §1/§8 はいずれも ITERATE 送りだが、DOM バインド規約は既に `figma_make_prompt.md` へ転記済みで空白ではない。M-18 と矛盾する記述（永続保存・キャッシュ・アクセス範囲の緩和を示唆する文言）は設計側のどこにも1文字も存在しない | 対策不要。ただし M-18 検証基準「アクセス可能範囲が担当チームに限定される」に対応する保持境界・権限設計は `design.md` §6 の ITERATE 記述時に必ず明文化すること（N/A ではなく次工程の必須事項として申し送り） |
| G2-2 | DOM バインド規約の貫通 | **PASS** | `figma_make_prompt.md` の Constraints 節に `innerHTML` / `outerHTML` / `insertAdjacentHTML` / `document.write` / `eval(` / `new Function(` / `dangerouslySetInnerHTML` / `v-html` の禁止識別子リストが `frontend-security.md` §2 と完全一致する形で掲載され、`textContent` / `createElement` / `addEventListener` / URL スキーム検証の規約も転記済み。`Guidelines.md` §1/§8 も同ファイルへの転記を明示し二重管理を避けている | ― |
| G2-3 | D-01（輝度極性: Light）のトレーサビリティ | **PASS** | `design.md` D-00 で D-01 を「非支配的」と判定（C-4 が OQ-2 として未確定、強い環境照度要因の記述なし）。D-01 本文は C-4 を明示引用し、決定モデル①の既定分岐を根拠に採用。棄却の論拠は「逆行させる根拠がない」と簡潔だが、`artifact-templates.md`「『非支配的』と判定したモデルは、フル ADR を書かない。1〜2行で済ませる」という明文の例外規定に合致した記述量であり、フル Mini-ADR（決定／根拠／導出プロセス／棄却した選択肢／再検討トリガの5点）を要求するのは規定over-runとなる。再検討トリガは `design.md` §10「OQ-1・OQ-2 が確定次第、再評価が必要」に明記されている | 対策不要（非支配的モデルの簡略記述として妥当）。ただし §10 の再検討トリガが D-01 本文と離れた場所にあり参照性が低いため、ITERATE で D-01 直下への転記を推奨 |
| G2-4 | D-02（操作アンカー: デスクトップ標準配置）のトレーサビリティ | **PASS** | 同上の論理。D-00 で「非支配的」と判定（C-3 が OQ-1 として未確定、把持・手袋・移動等の身体的制約記述なし）。D-02 本文は C-3 を明示引用。再検討トリガは §10 に共通記載。`figma_make_prompt.md` の D-02 記述（グローバル操作は上部固定、主要導線は各画面右上）と `design.md` の文言は完全一致 | G2-3 と同様、§10 参照性の改善を推奨 |
| G2-5 | screens.md ⇔ content.md：表示要素と文言の対応関係 | **FAIL** | screens.md の表示要素のうち、テーブル列見出し・フィールドラベル（件名・依頼者・チャネル・受信日時・問い合わせ概要・対応履歴・担当者・対応状況・引き継ぎ先・引き継ぎメモ・類似の過去問い合わせ見出し 等）に対応する `content.md` の T-id が存在しない。content.md は状態値・選択肢・ボタンラベル等「有限集合の語彙」はカバーしているが、構造的なフィールドラベルを欠いている。この欠落は `figma_output/01_2026-08-11` の実物照合（本レビューの IT-1-0、オーケストレータ実施）でも独立に検出されており、本レビューはそれを仕様レベルで裏付ける | **差し戻し先: `content.md`（design-agent）**。フィールドラベル群を T-id として追記する。既に `review_history.md` IT-1-0 で `IT-1-5` として design-agent への割り当てがスケジュール済みのため、本 FAIL はその実施根拠として扱ってよい |
| G2-6 | screens.md：Must Have（M-14, M-15）の機能的反映 | **FAIL** | requirements.md Must Have は「既存のSlack運用を活かせること（M-15）。検証: Slackでの問い合わせ・通知フローと併用可能」「依頼者へ進捗を伝えやすいこと（M-14）。検証: 対応状況を依頼者へ都度説明し直さずに共有できる」と定めるが、screens.md の Elements/Actions には Slack 通知・連携に相当する要素、および依頼者への進捗共有に相当する要素が無い。content.md T-03「Slack」は問い合わせのチャネル種別タグに過ぎず、M-15 の検証基準（通知フローとの併用）を満たす機能ではない | **差し戻し先: `screens.md`（design-agent）**。(a) M-15 を「チャネルタグとしての受動表示のみで足りる」という設計判断として明記するか、Slack 通知/連携の要素を追加する。(b) M-14 について、依頼者への進捗共有の具体的な導線（画面上の共有・エクスポート等）を Actions に追加するか、口頭/Slack 経由の手動共有で足りるとする設計判断を明記する。いずれも OQ-5（Slack活用の意図）の解消と合わせて次回商談で確定させること |
| G2-7 | screens.md ⇔ content.md：状態値のスコープ整合 | **FAIL**（軽微） | screens.md S01 は「対応状況での絞り込み」を検索・フィルタの表示要素に含めるが、content.md の対応状況値（T-11 未着手／T-12 対応中／T-13 対応済み）は「画面」列が S02 のみに限定されており、S01 での使用が明示されていない。この曖昧さが `figma_output` 側で「対応済みを含む」トグルのみの部分実装（IT-1-0 #7 として既記録）に帰結した可能性がある | **差し戻し先: `content.md`（design-agent）**。T-11〜T-13 の「画面」列を S01/S02 の両方に対応するよう修正するか、S01 用に同一文言の行を追加する |
| G2-8 | Guidelines.md ⇔ screens.md ⇔ content.md：「優先度」と「緊急度」の用語揺れ | **FAIL** | `requirements.md`（M-12 由来）は一貫して「緊急度」（緊急度の高い問い合わせ／検証: 緊急度による識別）と表記する。一方 `screens.md` は表示要素を「優先度バッジ」「優先度が設定されている問い合わせ」と表記しつつ、直後の説明文で「一覧上での緊急度の識別（M-12）」と“緊急度”も混在させる。`Guidelines.md` §2 も「緊急バッジ・優先度指標のみ」と1文中で両語を並記している。content.md T-06/T-07 の値自体は「緊急」「通常」の2値のみで、値の集合としては矛盾しないが、**上位概念のラベル（列見出し・バッジ名）がどちらの語で確定するのか文書間で一致していない** | **差し戻し先: `screens.md` / `Guidelines.md` / `content.md`（design-agent）**。要件の一次語である「緊急度」に統一するか、意図的に「優先度」へ言い換えるならその理由を `design.md` に ADR として残す。用語を1つに決め、`screens.md` の列見出し・`Guidelines.md` のトークン説明・`content.md` の項目名（種別列）を同一語で揃えること |
| G2-9 | Guidelines.md ⇔ figma_make_prompt.md ⇔ screens.md：トークン数値の一致 | **PASS** | `--target-primary`（高さ40px/最小幅160px、根拠「類似履歴を検索」7文字）は content.md T-16 の実文字数（7文字）と一致。`--target-choice`（高さ36px/最小幅96px、根拠「対応済み」4文字）は T-13 と一致。カラートークン5件は `check_contrast.py` による実測（IT-1-0 記録）で申告比率と完全一致（コントラスト比の再計算 PASS 済み）。figma_make_prompt.md はトークンを直接引用せず「Guidelines.md のトークンのみを使う」という参照型の記述にとどめており、数値の二重管理・食い違いは生じない | ― |
| G2-10 | フォールバック経路の一致（外部依存の失敗時挙動） | **N/A** | `design.md` §5/§7（例外制御フロー・状態遷移図）は ITERATE 送りで未生成。かつ本プロトタイプはバックエンド連携なし（`figma_make_prompt.md` Task 冒頭）のため、比較対象となる外部依存自体が現時点で存在しない | ITERATE で `design.md` §5/§7 を記述する段になった時点で改めて検証すること |
| G2-11 | design.md D-03/D-04 および未記入節 | **N/A** | `artifact-templates.md` の DRAFT 列で「―」指定の節（§4〜9 相当）であり、テンプレートどおり「ITERATE パスで記述」の1行のみ。存在しないものを欠陥として扱わない | ― |

### 検証ゲート2 総合判定: **FAIL（要差し戻し・停止）**

blocking ゲートのため、以下が解消されるまで次工程（tasks.md 生成等）への進行を止める。

1. **G2-5**: `content.md` にフィールドラベル群（件名・依頼者・チャネル・受信日時・問い合わせ概要・対応履歴・担当者・対応状況・引き継ぎ先・引き継ぎメモ 等）を T-id として追記する
2. **G2-6**: `screens.md` に M-14／M-15 の充足方法（機能追加、または受動表示で足りるという設計判断の明記）を追記する
3. **G2-7**: `content.md` の対応状況値（T-11〜T-13）の画面スコープを S01/S02 双方に対応させる
4. **G2-8**: 「優先度」／「緊急度」の用語を1つに統一し、`screens.md` / `Guidelines.md` / `content.md` の該当箇所を揃える

いずれも修正主体は design-agent（一部は product-agent への確認が前提: OQ-5）。G2-1〜G2-4, G2-9 は PASS、G2-10〜G2-11 は N/A であり、上記4点以外の再検証は不要。

### 再検証（2026-08-12・G2-5〜G2-8）

- レビュー日時: 2026-08-12（product-agent / design-agent 修正後）
- モード: ITERATE（blocking） / 実施者: quality-agent
- 対象: `requirements.md` / `design.md` / `screens.md` / `content.md` / `Guidelines.md` / `figma_make_prompt.md`（全文再読）

機械的な照合（「直したと書いてあるか」ではなく「実際に文書間で整合しているか」）を行った。

| # | 検査項目 | 再検証結果 | 根拠 |
|---|---|---|---|
| G2-5 | content.md にフィールドラベルの T-id が追加されているか | **解消（PASS）** | content.md に T-19（件名）／T-20（依頼者）／T-21（チャネル）／T-22（受信日時）／T-23（緊急度）／T-24（対応状況）／T-25（担当者）／T-28（問い合わせ概要見出し）／T-33（対応履歴見出し）／T-34（引き継ぎ先）／T-35（引き継ぎメモ）／T-36（類似の過去問い合わせ見出し）が追加されている。screens.md 側も S01 表示要素の記述で「文言は content.md T-19〜T-21, T-23〜T-25 を参照」と明示的に T-id を引用しており、双方向で対応が取れている。元の指摘にあったフィールドラベル群は全て充足 |
| G2-6 | screens.md に M-14/M-15 の充足方法が明記されているか | **解消（PASS）** | screens.md に新設された「設計メモ: M-14（依頼者への進捗共有）／M-15（Slack活用）の充足方法」節が、両者とも「受動表示・受動共有で暫定充足し、能動的な通知/連携機能は本イテレーションの Elements/Actions に含めない」という**設計判断**として明記している。requirements.md §3 Must Have の OQ-5 待ち記述（「受動的な共有で暫定的に充足するものとする」）と文言レベルで一致しており、上流の暫定合意を設計側が正しく引き継いでいる |
| G2-7 | content.md の対応状況値（T-11〜T-13）の画面スコープが S01/S02 双方に対応しているか | **解消（PASS）** | content.md の T-11／T-12／T-13 の「画面」列がいずれも `S01/S02` に修正されている。screens.md S01 の表示要素にある「対応状況・担当者の列は、S02 の表示要素と同一の状態値集合（content.md T-11〜T-13...）を用いる」という記述と整合する |
| G2-8 | 「優先度」／「緊急度」の用語が統一されているか | **解消（PASS）** | `grep -rn "優先度\|優先順位"` を実施した結果、`screens.md`（「文言は…参照。『優先度』ではなく『緊急度』で統一する」）・`Guidelines.md`（「『優先度』ではなく『緊急度』の表記へ統一する」）に残る「優先度」はいずれも**統一した旨のメタ的な言及**であり、実際の列見出し・バッジ名・トークン説明としての「優先度」表記は screens.md・content.md・Guidelines.md のいずれにも存在しない。UI ラベルとしての実体は全て「緊急度」（列見出し T-23）「緊急」（バッジ T-06）に統一されている。requirements.md §4.1 の「緊急度」記述も一次語のまま変わらず、文書間の表記は事実として一致した |

**検証ゲート2 再検証 総合判定: 解消済み（PASS）。ブロッキングは解除する。**

#### 付随的な軽微所見（新規検出・非ブロッキング）

G2-5〜G2-8 の再検証に加え、`design.md` の大幅加筆部分（§5〜9）について V-1〜V-4 の観点で軽く走査した。以下2点は新規に検出したが、いずれも当初の FAIL 4件の対象外であり、かつ次に挙げる理由により**今回のブロッキング対象には含めない**。次回イテレーションでの申し送り事項として記録する。

| # | 検出箇所 | 種別 | 内容 | 判断 |
|---|---|---|---|---|
| a | `content.md` T-31（未割り当て） | V-4 相当（G2-7 と同型） | T-31 の「画面」列が `S02` のみだが、`screens.md` は S01 の表示要素・表示の分岐でも T-31 を使用している（「担当者が未割り当ての問い合わせは、担当者列に『未割り当て』（content.md T-31）を表示する」）。T-11〜T-13 と同じ「画面スコープの不一致」が T-31 にだけ残存している | **非ブロッキング**。T-31 自体が requirements.md OQ-6 未解消の「仮説」値であり（content.md 補足に明記済み）、確定仕様として扱われていない。OQ-6 解消と合わせて次回修正するのが妥当で、今回の G2-5〜G2-8 の解消を妨げるものではない |
| b | `design.md` §5 ドメインロジック表 | V-1 に近い記述の曖昧さ（同一文書内） | `updateStatus` の例外列に「『対応済み』の問い合わせに対する `handoff` 呼び出し等…」という handoff 由来のシナリオが記載される一方、`handoff` 行の例外列には `InvalidStateTransitionError` の明記がない。§7 状態遷移図の注記（「handoff は…UI上抑止（InvalidStateTransitionError）」）と合わせて読むとどちらの操作がこの例外を投げるのか実装者が誤読しうる | **非ブロッキング**。上流要件との矛盾ではなく、同一文書内の記述整理の余地に留まる。次回 design.md 改訂時に `handoff` 行にも明記する形で解消するのが望ましい |

<!-- 人間のレビューコメント -->
<!-- レビュアー: -->
<!-- コメント: -->

---

## 開放的レビュー（2026-08-12）

V-1〜V-4 の検査項目にも、上記「保留指摘」にも該当しない観点で、成果物一式（`briefing.md` / `requirements.md` / `design.md` / `screens.md` / `content.md` / `Guidelines.md` / `figma_make_prompt.md` / `figma_output/01_2026-08-11` 実物）を通読し、最も改善効果が大きいと考えた3件を判定する。

### 1. M-18 のセキュリティ検証基準が、引き渡し後に検証不能な文言のままになっている

- **観察**: requirements.md Must Have M-18 の検証基準は「アクセス可能範囲が担当チームに限定される」。design.md §6 はこれを設計原則として採用しつつ、「認証・認可の実装自体は実装フェーズへ委譲する」と明記している（D-04 と整合した判断であり、それ自体は正しい）。しかし、この委譲の結果、**本フレームワークが引き渡す `tasks.md` の DoD にこの検証基準をどう落とし込むかが、現状どこにも定義されていない**。認証基盤が無い前提で「アクセス可能範囲が担当チームに限定される」ことをどう検証可能な文で表現するかが空白になっている
- **判断: 採用**。design.md には矛盾はなく差し戻し事項ではないが、放置すると Bolt の DoD が「認証を実装する」という本フレームワーク範囲外の主観的な文言に流れてしまう。tasks.md 生成時（本レビューのタスク3）に、UI 設計で担保できる範囲（チーム外共有導線が存在しないことの確認など）に限定した検証可能な DoD として反映する

### 2. `figma_output/01_2026-08-11` の実物が、本イテレーションで確定した「緊急度」表記に追随していない

- **観察**: 回収済みプロトタイプ `App.tsx` を実際に読むと、型名 `Priority`（L7）、S01 テーブルの列見出し配列 `['件名', '依頼者', 'チャネル', '優先度', '対応状況', '担当者']`（L584）、新規登録モーダルの `<legend>優先度</legend>`（L717）と、**「優先度」という語が UI ラベルとして3箇所に実在**している。これは G2-8 が対象とした仕様文書（screens.md / Guidelines.md / content.md）の話ではなく、既に回収済みの**実装物**の話であり、仕様側の修正がまだ実物へ反映されていないことを意味する
- **判断: 採用**。次回 Figma Make 再生成時に反映されるべき既知の乖離として、`README.md` の申し送り事項に明記する（ゲート3・実物チェックの対象として記録し、「仕様は直したが実物は古いまま」という状態を放置しない）

### 3. 「未割り当て」問い合わせの放置リスクに対する視覚的エスカレーションが未検討

- **観察**: M-4「誰が対応しているか分からず、放置や重複対応が発生する」は課題の核心の一つ。担当者が「未割り当て」のまま一覧に埋もれた場合、緊急度バッジのような視認性の強化（色・並び順での優先表示等）が screens.md/design.md のどこにも用意されていない。「未割り当てのまま長時間放置される」リスクは M-4 の再発に直結しうる
- **判断: 不採用**。「未割り当て」状態自体が requirements.md OQ-6 として未解決の仮説であり、design.md §10 も「確定仕様として扱わない」と明記している。**確定していない状態に対して追加の可視化機能を積み増す設計判断を今回下すのは時期尚早**であり、YAGNI・仮説先行の原則（design.md 全体が慎重に守っている姿勢）に反する。OQ-6 が解消され「未割り当て」が正式な状態として確定した時点で、次回設計判断として改めて検討すべき論点として記録するに留める（新規の Open Question としては起票せず、OQ-6 の解消時に併せて検討する事項として本レビューに記録するのみとする）
